<?php
    // +只能是算术运算符，系统会自动的将+两边的变量转换为数组，如果可以转换，就进行转换，否则返回这个数据类型的默认值  int >0 object > null bool>false float:0.0
    // 在php中字符串连接符使用.

    // 算术运算符： + - * 、 % ++ --
    // 赋值运算符：= += -= *= /=
    // 逻辑运算符：！ && ||
    // 比较运算符：> >= < <= == === !=
    // 三元运算符：a>b ? "" : ""
?>